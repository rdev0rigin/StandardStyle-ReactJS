import * as React from 'react';

export const LandingComponent = () => {
	return(
		<div>
			<h1>What is Standard Styles</h1>
			<p>
				Here you have a prototype of a single source of truth for white labeling projects cross-platform.
				The interface here updates a single source of truth which then propagates style values to all
				applications within scope.
			</p>
		</div>
	);
};
